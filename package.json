{
  "name": "starfinders",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "fs-extra": "^7.0.1",
    "ncp": "^2.0.0",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-modal": "^3.8.1",
    "react-scripts": "2.1.3",
    "rimraf": "^2.6.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "clean": "rm asset-manifest.json && rm favicon.ico && rm precache-* && rm service-* && rm index.html"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
